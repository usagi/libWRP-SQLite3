box: wercker/ubuntu
build:
  steps:
    - script:
      - sudo apt install -y clang libsqlite3-dev cmake ninja-build
      - mkdir build && cd build
      - cmake -G Ninja -DCMAKE_CXX_COMPILER=clang++ -DCMAKE_BUILD_TYPE=release ..
      - ninja
